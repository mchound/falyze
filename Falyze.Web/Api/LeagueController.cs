using Falyze.Cache;
using Falyze.Data;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace Falyze.Web.Api
{
    public class LeagueController : ApiController
    {
        //[HttpGet]
        //public HttpResponseMessage Get()
        //{
        //    StatisticsService service = new StatisticsService(new Repository(new BetterDatabase()), new StatisticManager(), new CacheService());
        //    return Request.CreateResponse<IEnumerable<Falyze.Data.Models.League>>(HttpStatusCode.OK, service.GetLeagues());
        //}
    }
}
